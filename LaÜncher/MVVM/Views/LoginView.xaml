<Window x:Class="USofware_HUb.MVVM.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:USofware_HUb.MVVM.Views"
        xmlns:viewModel="clr-namespace:USofware_HUb.MVVM.ViewModel"
        Title="LaÜncher Logowanie"
        Width="400"
        Height="300"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="#FF1A1A1A"
        AllowsTransparency="True"
        ResizeMode="NoResize">

    <Window.DataContext>
        <viewModel:LoginViewModel/>
    </Window.DataContext>

    <Grid RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <ScaleTransform x:Name="fadeScale"
                                ScaleX="0.8"
                                ScaleY="0.8"/>
            </Grid.RenderTransform>

            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <TextBlock Text="Witaj w HÜbie"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0,0,0,20"
                           HorizontalAlignment="Center"/>

                <Button Content="Zaloguj się" Command="{Binding LoginCommand}" Style="{StaticResource WideButtonStyle}" Margin="0,5" IsEnabled="False"/>
                <Button Content="Zarejestruj się" Command="{Binding RegisterCommand}" Style="{StaticResource WideButtonStyle}" Margin="0,5" IsEnabled="False"/>
                <Button Content="Konto offline" Command="{Binding OfflineCommand}" Style="{StaticResource WideButtonStyle}" Margin="0,5"/>
                <Button Content="Wyjdź" Command="{Binding QuitCommand}" Style="{StaticResource WideButtonStyle}" Margin="0,5"/>
        </StackPanel>
        </Grid>
    </Window>
